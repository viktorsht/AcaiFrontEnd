import 'package:flutter/material.dart';

class RecipientePage extends StatelessWidget {
  const RecipientePage({super.key});

  @override
  Widget build(BuildContext context) {
    return PageView(
      children: [
        //Container(color: Colors.amber, height: 30,),
        //Container(color: Colors.amber, height: 30,),
      ],
    );
  }
}